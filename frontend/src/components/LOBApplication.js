import { Box, Typography, Paper } from '@mui/material';
export const LOBApplication = () => (
  <Box>
    <Paper sx={{ p: 2, mb: 3 }}>
      <Typography variant='h6'>LOB Application</Typography>
      {/* Add LOB Application related components here */}
    </Paper>
  </Box>
);
