import { Box, Typography, Paper } from '@mui/material';

export const DBScanner = () => (
  <Box>
    <Paper sx={{ p: 2, mb: 3 }}>
      <Typography variant='h6'>DB Scanner</Typography>
      {/* Add DB Scanner related components here */}
    </Paper>
  </Box>
);
